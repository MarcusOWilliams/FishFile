Making the self reference using fathered and father_id (also mother) in the Fish Database

Making subqueries work
    Subquery, selcet from etc.

Adding pagination
    Especially to changes because you had to start with an empty query, then union then, sort, then paginate all, nothing else works

make allele changes work in a user friendly way

Making the screen synamic to be nice on all screens

Sending delete forms with javascript post requests

Making custom codes and license
    Needed to make checks for if there was user codes
    Needed to use javascript for forms and correct validation
    making usre there was only one of each and the user couldn't be confused which

Adding stock total on custom Date
    Making it work once the system had already been created